{
  "name": "ci-parity-doctor-mcp",
  "version": "0.1.0",
  "description": "MCP server for CI reproducibility and parity diagnostics",
  "type": "module",
  "main": "dist/cli.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "node dist/cli.js serve",
    "smoke": "node dist/cli.js smoke",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.21.2",
    "fast-glob": "^3.3.3",
    "yaml": "^2.8.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
